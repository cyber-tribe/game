{
  "name": "garudo-dungeon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "少年ガルドと迷いの洞窟 — ターン制で潜る 3D ローグライク",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run --config vitest.e2e.config.ts",
    "typecheck": "tsc --noEmit",
    "models": "tools/venv/bin/python tools/build_models.py",
    "audio": "node tools/audio/build.ts",
    "playtest": "node tools/playtest.mjs",
    "preview-engine": "node tools/preview_engine.mjs",
    "auto-tester": "node tools/auto-tester.mjs",
    "cap:sync": "npm run build && cap sync",
    "cap:add:android": "cap add android",
    "cap:add:ios": "cap add ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@types/node": "^26.2.0",
    "@types/three": "^0.185.4",
    "gifenc": "^1.0.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=22.12"
  }
}
