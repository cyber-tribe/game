> **実装済み。** `tools/models/monsters.py`に`build_yoroioiteke()`/
> `yoroioiteke_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py yoroioiteke`で
> `public/models/yoroioiteke.glb`を書き出し)。
>
> **骨格は計画書どおり`honegarami`と同じ人型骨組みをベースにした。**
> 未決事項にあった「アーマチュアの流用可否」は、四肢を太くし低い重心の
> どっしりした体格に組み替えるだけで問題なく流用できることを確認した。
>
> **「置いていかれる恐れそのものを鎧に変えた」由来を、全身を覆う一枚板
> の鎧(胸当て・背当て・腰当て・肩当て・小手・すね当て・閉じた兜)で
> 表現した。** 同じ第四地方の`yoroimukade`(節状装甲・むき出しの牙と
> 眼窩)とはっきり見分けがつくよう、素肌をほとんど見せない重装の
> シルエットにし、兜は面頬(バイザー)の細いスリットの奥にわずかに光る
> だけにした。「被弾のたびに相手の満腹度を削り返す」由来として、鎧の
> 各所(兜の頂・肩当て・小手・膝)に赤くにぶく光る棘(見返す刃)を並べた。
> 装甲の各パーツは、下地の体表よりはっきり張り出させることで一枚板の
> 鎧らしい輪郭になるよう調整した(最初の実装では体表に埋もれて見えづらく、
> サイズと張り出し量を修正した)。
>
> **配色は計画書どおり、第四地方(骨積みの回廊)の白骨色・くすんだ灰色。**
>
> **三角形数は5,748**(既存モデルの目安1,800〜7,500の範囲内)。
>
> **アニメーションは`honegarami_animations()`を流用せず、新規に
> キーフレームを書いた。** `idle`は重い鎧のままわずかに身構える動き、
> `attack`は鎧の棘を突き出して押し返すような重く短い一撃、`hit`は
> 高い防御力どおりほとんど揺るがない動きにした(クリップ構成・ボーン名は
> 変えていない)。
>
> **見た目の確認は`tools/preview/yoroioiteke.png`のCyclesレンダーで
> 行った。** 裏返り・体積ゼロ・パーツの誤結合といった破綻はない。
> `tools/compare_models.mjs`で全モデル(82個)を再ビルドして構造(頂点数・
> クリップ名・外形)を突き合わせ、一致することも確認した。
>
> **`src/entities/species.ts`の`yoroioiteke.model`を`"honegarami"`→
> `"yoroioiteke"`に変更した。** `yoroioiteke`は
> `tests/monster-roster-expansion-species.test.ts`の60種化ロースター拡張
> (`NEW_SPECIES_IDS`)には含まれていない種族のため、`KNOWN_MODEL_IDS`の
> 更新は不要だった。
>
> `npx tsc --noEmit`・`npx vitest run`(1259/1259)・`npm run build`は
> すべてgreen。未決事項だった「最終的な三角形数・関節配置の微調整」は
> 上記の数値で確定させた。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。

# ヨロイオイテケ(yoroioiteke)の3Dモデル

現在は`honegarami`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

防御が上がり、被弾のたびに相手の満腹度を削り返すようになった姿。置いていかれる恐れそのものを鎧に変えた。

AI挙動(`guard`)に合わせ、厚い甲羅や装甲質の表皮、低い重心のどっしりした形にする。
配色は第4地方(骨積みの回廊)のテーマに合わせ、
積み重なった骨を思わせる、白骨色・くすんだ灰色を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`honegarami`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`honegarami`モデルを基準に、ステータス(maxHp 72)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`honegarami`のアーマチュアをそのまま流用できるか、一部調整が要るか
