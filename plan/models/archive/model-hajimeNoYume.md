> **実装済み。** `tools/models/monsters.py`に`build_hajimeNoYume()`/
> `hajimeNoYume_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py hajimeNoYume`で
> `public/models/hajimeNoYume.glb`を書き出し)。
>
> **骨格は計画書どおり`madoromi`と同じ関節構成(root-stem-capbase-captop)を
> そのまま流用した。** 未決事項にあった「アーマチュアの流用可否」は、
> 座標・半径の差し替えだけで問題なく流用できることを確認した
> (`oitekeboshi`と同じ前例)。melee AIにふさわしい「がっしりした体格で
> 正面から迫る力強いシルエット」は、幹(root-stem)の半径をcapbaseに
> 近い太さのまま保って先細りを抑えることで表現し、新規ボーンの追加は
> していない。
>
> **配色は計画書どおり、第八地方(めざめの前庭)のテーマ「第一〜第七
> 地方の色が淡く混ざり合った、統一感のない配色」を造形に落とし込んだ。**
> `yorishironozankyo`(ヨリシロの残響、同じ第八地方テーマを高さ方向の
> 5色帯で表現済み)とかぶらないよう、hajimeNoYumeは傘の面を中心角度で
> 7分割する放射状のパッチワークにし、第一〜第七地方それぞれの代表色を
> 淡くしたもの(空色・緑・紅紫・白骨色・青灰・土色・紅)を割り当てた。
> 幹(root-stem、capbaseの途中まで)はどの地方色にも染めず、生成り色の
> ままにして「まだ何も枝分かれしていない起点」を表した。傘の色分割
> ぶんに1本ずつ、`cone()`の小さな芽を生やし(Z軸沿いにしか作れない
> 制約から、ドーム曲面に沿わせて真上に伸ばす`yamabikooni`の角と同じ
> 手法)、「他のすべての夢のかけらは、この最初の夢から枝分かれして
> 生まれた」という由来を視覚化した。目は`madoromi`の半開きの眠たげな
> 目とは対照的に、`eyeball()`でしっかり見開いたものにした。根元には
> 不揃いな間隔で根の塊(`uv_sphere`)を5つ配置し、統一感のなさを
> 根元にも一貫させつつ、melee AIの正面からのどっしりした構えを支える
> 土台にした。
>
> **三角形数は2,800**(既存モデルの目安1,800〜7,500の範囲内)。maxHp
> 260という高いステータスに見合うよう、`madoromi`(半径: root 0.115 /
> stem 0.095 / capbase 0.275 / captop 0.055、高さ0.50)より一回り以上
> 大きく育てた(半径: root 0.190 / stem 0.170 / capbase 0.440 /
> captop 0.090、高さ0.88)。全高は`yorishironozankyo`(高さ1.045)・
> `yamabikooni`(高さ0.905)に匹敵する、本作でも最大級のシルエットに
> なっている。
>
> **アニメーションは計画書どおり、`madoromi_animations()`の5クリップ
> 構成(待機・歩行・攻撃・被弾・消滅)をそのまま踏襲しつつ、キーフレーム
> は新規に書いた。** `attack`は幹全体をひねり込んで正面から重く
> のしかかる一撃、`die`は最初の夢が解けるように巨体がゆっくり大きく
> 崩れ落ちる動きにした(ボーン名・クリップ構成は変えていない)。
>
> **見た目の確認は`tools/preview/hajimeNoYume.png`のCyclesレンダーで
> 行った。** 裏返り・体積ゼロ・パーツの誤結合といった破綻はない。
> glTFエクスポート時に出る`Mesh hajimeNoYume is not valid`という
> 警告は、自動ウェイトの正規化誤差(合計1.000003)によるもので、
> 同じ警告が`madoromi`本体の再ビルドでも出ることを確認済みの、
> 無害な既知の挙動。`tools/build_models.py --no-preview`で全62個
> (モンスター49種+その他の小道具モデル)を再ビルドし、
> `hajimeNoYume.glb`以外に実質的な差分がないこと(タイムスタンプ等の
> 非決定的なバイト差分のみで、`git checkout`で復元)を確認した。
>
> **`src/entities/species.ts`の`hajimeNoYume.model`を`"madoromi"`→
> `"hajimeNoYume"`に変更した。** `hajimeNoYume`は
> `tests/monster-roster-expansion-species.test.ts`の60種化ロースター拡張
> (`NEW_SPECIES_IDS`)には含まれていない種族のため、`KNOWN_MODEL_IDS`の
> 更新は不要だった(`oitekeboshi`と同様の扱い)。
>
> `npx tsc --noEmit`・`npx vitest run`(1219/1219)・`npm run build`は
> すべてgreen。未決事項だった「最終的な三角形数・関節配置の微調整」は
> 上記の数値で確定させた。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。

# はじめの夢(hajimeNoYume)の3Dモデル

現在は`madoromi`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

ヨリシロがこの世でいちばん最初に見た夢そのものが、ひとり分の姿を取ったもの。他のすべての夢のかけらは、この最初の夢から枝分かれして生まれた。

AI挙動(`melee`)に合わせ、がっしりした体格で、正面から迫る力強いシルエットにする。
配色は第8地方(真の目覚め)のテーマに合わせ、
第一〜第七地方の色が淡く混ざり合った、統一感のない配色を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`madoromi`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`madoromi`モデルを基準に、ステータス(maxHp 260)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`madoromi`のアーマチュアをそのまま流用できるか、一部調整が要るか
