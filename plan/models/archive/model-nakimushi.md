> **実装済み。** `tools/models/monsters.py`に`build_nakimushi()`/
> `nakimushi_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py nakimushi`で
> `public/models/nakimushi.glb`を書き出し)。
>
> **骨格は計画書どおり`tsubute`と同じ関節構成をベースにした。**
> 未決事項にあった「アーマチュアの流用可否」は、関節名・ボーン構成を
> そのまま使い、座標だけをおよそ6割に縮めて問題なく流用できることを
> 確認した。「単体は簡略化した小さなシルエット(複数体まとめて配置
> される前提)」の方針どおり、既存の`tsubute`より一回り小さくまとめた。
>
> **「泣きやまない小さな夢」の由来を、大きく見開いた目・頬を伝う涙の
> 筋・大きく開けた泣き顔の口で表現した。** 目は`eyeball()`を流用し、
> 涙の筋は目の下に縦長の`uv_sphere`を1つずつ添えて表した。
>
> **配色は計画書どおり、涙と滝つぼを思わせる沈んだ青・藍色系。**
>
> **三角形数は4,708**(既存モデルの目安1,800〜7,500の範囲内)。
>
> **アニメーションは`tsubute_animations()`を流用せず、新規に
> キーフレームを書いた。** `idle`はしゃくり上げるような小刻みな震え、
> `attack`は小さな体を反らせて精一杯泣き声を上げる動きにした
> (クリップ構成・ボーン名は変えていない)。
>
> **見た目の確認は`tools/preview/nakimushi.png`のCyclesレンダーで
> 行った。** 裏返り・体積ゼロ・パーツの誤結合といった破綻はない。
> `tools/compare_models.mjs`で全モデル(57個)を再ビルドして構造(頂点数・
> クリップ名・外形)を突き合わせ、一致することも確認した。
>
> **`src/entities/species.ts`の`nakimushi.model`を`"tsubute"`→
> `"nakimushi"`に変更した。** `nakimushi`は60種化ロースター拡張
> (`plan/game/archive/monster-roster-expansion-species.md`)由来の種族でも
> あるため、`tests/monster-roster-expansion-species.test.ts`の
> `KNOWN_MODEL_IDS`に`nakimushi`を追記し、直前のコメントも更新した。
>
> `npx tsc --noEmit`・`npx vitest run`(1190/1190)・`npm run build`は
> すべてgreen。未決事項だった「最終的な三角形数・関節配置の微調整」は
> 上記の数値で確定させた。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。

# なきむし(nakimushi)の3Dモデル

現在は`tsubute`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

泣きやまない小さな夢。群れで現れ、仲間が倒されるたびに、残ったものの声がいっそう大きくなる。

AI挙動(`swarm`)に合わせ、単体は簡略化した小さなシルエット(複数体まとめて配置される前提)にする。
配色は第5地方(なみだの滝つぼ)のテーマに合わせ、
涙と滝つぼを思わせる、沈んだ青・藍色系を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`tsubute`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`tsubute`モデルを基準に、ステータス(maxHp 16)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`tsubute`のアーマチュアをそのまま流用できるか、一部調整が要るか
