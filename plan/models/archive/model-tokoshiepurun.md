> **実装済み。** `tools/models/monsters.py`に`build_tokoshiepurun()`/
> `tokoshiepurun_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py tokoshiepurun`で
> `public/models/tokoshiepurun.glb`を書き出し)。
>
> **骨格は計画書どおり`purun`と同じ縦2本の関節構成をベースにした。**
> 未決事項にあった「アーマチュアの流用可否」は、座標をおよそ1.15倍に
> するだけで問題なく流用できることを確認した。「姿はぷるんのまま」
> という方針どおり、形そのものはほとんど変えず、根元に揺るがない
> 土台を思わせる薄い輪を足しただけにとどめた。
>
> **「揺るぎなさだけが増している」由来を、根元の輪と、被弾しても
> ほとんど揺るがない`hit`アニメーションで表現した。**
>
> **配色は計画書どおり、第一地方(うたたねの参道)の参道の土色に
> 馴染む素朴な淡い色合い**(`purun`本来の青系統から変更)。
>
> **三角形数は1,888**(既存モデルの目安1,800〜7,500の範囲内、
> 目安下限に近いが範囲内)。
>
> **アニメーションは`purun_animations()`をそのまま流用せず、`hit`だけ
> 「みをまもる」の常時発動性質に合わせて控えめな縮みに変更し、他の
> クリップはpurunと同じキーフレーム構成を踏襲した**(クリップ構成・
> ボーン名は変えていない)。
>
> **見た目の確認は`tools/preview/tokoshiepurun.png`のCyclesレンダーで
> 行った。** 裏返り・体積ゼロ・パーツの誤結合といった破綻はない。
> `tools/compare_models.mjs`で全モデル(78個)を再ビルドして構造(頂点数・
> クリップ名・外形)を突き合わせ、一致することも確認した。
>
> **`src/entities/species.ts`の`tokoshiepurun.model`を`"purun"`→
> `"tokoshiepurun"`に変更した。** `tokoshiepurun`は
> `tests/monster-roster-expansion-species.test.ts`の60種化ロースター拡張
> (`NEW_SPECIES_IDS`)には含まれていない種族のため、`KNOWN_MODEL_IDS`の
> 更新は不要だった。
>
> `npx tsc --noEmit`・`npx vitest run`(1251/1251)・`npm run build`は
> すべてgreen。未決事項だった「最終的な三角形数・関節配置の微調整」は
> 上記の数値で確定させた。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。

# とこしえのぷるん(tokoshiepurun)の3Dモデル

現在は`purun`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

まどろみの余韻を重ねすぎた結果、被弾を和らげる性質が常に発動するようになった姿。姿はぷるんのままだが、揺るぎなさだけが増している。

AI挙動(`melee`)に合わせ、がっしりした体格で、正面から迫る力強いシルエットにする。
配色は第1地方(うたたねの参道)のテーマに合わせ、
参道の土色に馴染む、素朴な淡い色合いを基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`purun`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`purun`モデルを基準に、ステータス(maxHp 22)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`purun`のアーマチュアをそのまま流用できるか、一部調整が要るか
