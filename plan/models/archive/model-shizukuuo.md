> **実装済み。** `tools/models/monsters.py`に`build_shizukuuo()`/
> `shizukuuo_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py shizukuuo`で
> `public/models/shizukuuo.glb`を書き出し)。
>
> **骨格は計画書どおり`tsubute`と同じ関節構成をベースにした。**
> 未決事項にあった「アーマチュアの流用可否」は、関節名・ボーン構成を
> そのまま使い、座標だけを差し替える形で問題なく流用できることを
> 確認した。四足の蛙ではなく水滴形の魚にするため、`armF/handF`を
> 小さな胸びれに、`kneeB/ankleB/footB`を後方へ長く伸ばして尾びれに、
> `head`を先細りの水滴の先端にした。
>
> **配色は計画書どおり、涙と滝つぼを思わせる沈んだ青・藍色系。**
> `roughness`を低くして、しずくらしい艶やかな質感にした。
>
> **三角形数は3,760**(既存モデルの目安1,800〜7,500の範囲内)。
>
> **アニメーションは`tsubute_animations()`を流用せず、新規に
> キーフレームを書いた。** `walk`は尾びれを大きくくねらせて泳ぐ動きに、
> `die`はしずくが弾けるように尾びれを大きく振り切って消える動きに
> した(クリップ構成・ボーン名は変えていない)。
>
> **見た目の確認は`tools/preview/shizukuuo.png`のCyclesレンダーで
> 行った。** 裏返り・体積ゼロ・パーツの誤結合といった破綻はない。
> `tools/compare_models.mjs`で全モデル(52個)を再ビルドして構造(頂点数・
> クリップ名・外形)を突き合わせ、一致することも確認した。
>
> **`src/entities/species.ts`の`shizukuuo.model`を`"tsubute"`→
> `"shizukuuo"`に変更した。** `shizukuuo`は
> `tests/monster-roster-expansion-species.test.ts`の60種化ロースター拡張
> (`NEW_SPECIES_IDS`)には含まれていない種族のため、`KNOWN_MODEL_IDS`の
> 更新は不要だった。
>
> `npx tsc --noEmit`・`npx vitest run`(1175/1175)・`npm run build`は
> すべてgreen。未決事項だった「最終的な三角形数・関節配置の微調整」は
> 上記の数値で確定させた。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。

# しずくうお(shizukuuo)の3Dモデル

現在は`tsubute`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

こぼれ落ちる涙のしずくそのもの。1匹では儚いが、涙は次々こぼれるように群れで現れる。

AI挙動(`swarm`)に合わせ、単体は簡略化した小さなシルエット(複数体まとめて配置される前提)にする。
配色は第5地方(なみだの滝つぼ)のテーマに合わせ、
涙と滝つぼを思わせる、沈んだ青・藍色系を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`tsubute`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`tsubute`モデルを基準に、ステータス(maxHp 20)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`tsubute`のアーマチュアをそのまま流用できるか、一部調整が要るか
