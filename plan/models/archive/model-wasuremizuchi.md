> **実装済み。** `tools/models/monsters.py`に`build_wasuremizuchi()`/
> `wasuremizuchi_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py wasuremizuchi`で
> `public/models/wasuremizuchi.glb`と`tools/preview/wasuremizuchi.png`を
> 書き出し)。先行するworktreeと同じく、既に用意済みのvenv・node_modulesを
> シンボリックリンクして再利用し、再ダウンロードは避けた。
>
> **骨格は計画書どおり、現在流用中の`tsubute`と同じ関節の"種類"
> (胴の芯+頭+腕+脚をつなぐ7本の骨)をそのまま流用した。** 未決事項
> だった「`tsubute`のアーマチュアをそのまま流用できるか」への回答は
> 「関節の種類・ボーン構成はそのまま使えるが、座標と半径は総入れ替え
> が必要」というもの。`tsubute`はずんぐりした蛙の体格に合わせて胴・
> 手足の半径が太いため、そのままでは「小柄で華奢な、逃げ足の速さを
> 感じさせる軽いシルエット」にならない。胴・頭・手足の半径をはっきり
> 細く絞り、関節どうしの間隔も詰めて全体を一回り小さくまとめた。
>
> **最初の試作では、腕・脚の付け根(`armF`/`kneeB`)が胴の中心からの
> 距離のわりに半径を数値上わずかしか超えて突き出ておらず(0.002程度)、
> `assign_materials_by_region`を関節からの距離で判定すると、手足の
> すぐ根元の胴の面まで軒並み手足側の色に呑み込まれてしまう問題が
> 起きた(手足の判定面積が全体の80.5%を占めてしまい、実際の面数を
> 数えて気づいた)。** `armF.L`/`kneeB.L`を胴の中心から明確に離す
> (親の半径をはっきり超えて突き出るようにする)よう座標を引き直した
> うえで、色分けの距離しきい値も0.050→0.033に絞り、背56%・腹5%・
> 手足40%程度の妥当な比率に収まったことを確認した。
>
> **配色は計画書どおり、霧と水を思わせる灰みがかった水色・青緑系。**
> 背は明るい霧色、腹は沈んだ暗い青灰色、手足は実体を失いかけた霧
> そのもののような淡い色、の3トーンに塗り分けた。加えて、頭の後ろ
> から尾のように霧が尾を引く演出として、`uv_sphere`を先端ほど小さく
> ・発光を強めて4つ並べ、「すっかり忘れ去られた」「いつでも深みへ
> 逃げ込める」という設定を視覚化した。頭の両脇には薄い膜状のひれを
> `box`で1枚ずつ足している。**手動での頂点シアーは使っていない**
> (mabutamushiで壊れた反省どおり、primitiveの組み合わせのみで作った)。
>
> **三角形数は5,808**。既存モデルの目安(1,800〜7,500)の範囲内。
>
> **見た目の確認は`tools/preview/wasuremizuchi.png`のCyclesレンダーに
> 加え、ヘッドレスブラウザでの実機確認も行った。** `tools/playtest.mjs`
> と同じ構成(playwright+SwiftShader)で臨時のスクリプトを組み、
> `debugDescend()`で9階(わすれみずちの`minFloor`)以降まで潜り、実際に
> 出現したわすれみずちをプレイヤーの正面に立たせてスクリーンショットで
> 確認した。裏返り・体積ゼロ・パーツの誤結合といった破綻はなく、
> ページ内エラーも0件だった(臨時スクリプトは確認後に削除済み)。
>
> **`src/entities/species.ts`の`wasuremizuchi.model`を`"tsubute"`→
> `"wasuremizuchi"`に変更した。** `tests/monster-roster-expansion-species.test.ts`
> の`KNOWN_MODEL_IDS`に`"wasuremizuchi"`を追記した。`npx tsc --noEmit`・
> `npx vitest run`(1115/1115)・`npm run build`はすべてgreen。
>
> **未決事項だった「最終的な三角形数・関節配置の微調整」は上記の数値・
> 構成で確定させた。** 対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。

# わすれみずち(wasuremizuchi)の3Dモデル

現在は`tsubute`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

すっかり忘れ去られた水霊。モヤウツボの成れの果てに近い存在で、触れられるとすぐ深みへ逃げ込み、追う者の足を止める。

AI挙動(`coward`)に合わせ、小柄で華奢な、逃げ足の速さを感じさせる軽いシルエットにする。
配色は第2地方(忘れ潮の湿地)のテーマに合わせ、
霧と水を思わせる、灰みがかった水色・青緑系を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`tsubute`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`tsubute`モデルを基準に、ステータス(maxHp 16)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`tsubute`のアーマチュアをそのまま流用できるか、一部調整が要るか
