> **実装済み。** `tools/models/monsters.py`に`build_misemonoNoNushi()`/
> `misemonoNoNushi_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py misemonoNoNushi`で
> `public/models/misemonoNoNushi.glb`を書き出し)。
>
> **骨格は計画書どおり`honegarami`と同じ人型骨組みをベースにした。**
> 未決事項にあった「アーマチュアの流用可否」は、`yamabikooni`と同じく
> 座標をボスサイズまで拡大するだけで問題なく流用できることを確認した。
>
> **地方の他の種族の意匠を1体に集約し、「見世物のぬしの小型版」である
> `kazaridaruma`より大きく力強い存在感にした。** `menkaburikozo`譲りの
> 祭り面(より大きく厚みを持たせた)、`honedatami`譲りの重ねた板(肩の
> ケープと胸の勲章のような装飾)、`kazaridaruma`譲りの金色の帯を
> 組み合わせた。
>
> **試作段階で、祭り面の目の穴が埋もれて見えなくなる不具合があった。**
> 目の穴(`uv_sphere`)を面の中心からの距離で「前に突き出す」位置に
> 置いたつもりが、実際には面本体の表面(中心からY方向に0.053ぶん)より
> 内側に収まってしまい、レンダリングでは見えなかった。面の表面より
> さらに手前(Y方向へ-0.238、面表面の-0.223より外側)へ押し出すことで
> 解決した。
>
> **配色は計画書どおり、くすんだ紅色と金色の名残。**
>
> **三角形数は5,196**(既存モデルの目安1,800〜7,500の範囲内)。
>
> **アニメーションは`honegarami_animations()`・`yamabikooni_animations()`
> を流用せず、新規にキーフレームを書いた。** 「かつて客を呼び込んだ
> 両腕を大きく広げてから、力強く叩きつける」`attack`にし、`idle`は
> 誰もいない会場に居座り続ける堂々とした待機にした(クリップ構成・
> ボーン名は`hip-chest`等honegarami由来のまま)。
>
> **見た目の確認は`tools/preview/misemonoNoNushi.png`のCyclesレンダーで
> 行った。** 修正後は目の穴・金色の縁取り・肩のケープ・胸の勲章が
> はっきり見て取れる。裏返り・体積ゼロ・パーツの誤結合といった破綻は
> ない。`tools/compare_models.mjs`で全モデル(48個)を再ビルドして構造
> (頂点数・クリップ名・外形)を突き合わせ、一致することも確認した。
>
> **`src/entities/species.ts`の`misemonoNoNushi.model`を`"honegarami"`→
> `"misemonoNoNushi"`に変更した。** `misemonoNoNushi`は地方ボス
> (`minFloor: Infinity`・`weight: 0`)のため
> `tests/monster-roster-expansion-species.test.ts`の60種化ロースター拡張
> (`NEW_SPECIES_IDS`)には含まれておらず、`KNOWN_MODEL_IDS`の更新は
> 不要だった。
>
> `npx tsc --noEmit`・`npx vitest run`(1167/1167)・`npm run build`は
> すべてgreen。未決事項だった「最終的な三角形数・関節配置の微調整」は
> 上記の数値で確定させた。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。
>
> **これで第七地方(わすれられた祭りの跡)の全種族(めんかぶりこぞう・
> かざりだるま・かげぼうし・ちょうちんおくり・わたあめのおばけ・
> やぐらもり・見世物のぬし)の専用モデルが揃った。**

# 見世物のぬし(misemonoNoNushi)の3Dモデル

現在は`honegarami`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

かつての祭りでもっとも人目を引いた出し物の記憶が、朽ちてなお色濃く残った姿。今は誰もいない会場の中央に、当時のままの存在感で居座り続ける。

AI挙動(`melee`)に合わせ、がっしりした体格で、正面から迫る力強いシルエットにする。
配色は第7地方(わすれられた祭りの跡)のテーマに合わせ、
褪せた祭りを思わせる、くすんだ紅色・金色の名残を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`honegarami`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`honegarami`モデルを基準に、ステータス(maxHp 152)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`honegarami`のアーマチュアをそのまま流用できるか、一部調整が要るか
