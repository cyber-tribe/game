> **実装済み。** `tools/models/monsters.py`に`build_menkaburikozo()`/
> `menkaburikozo_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py menkaburikozo`で
> `public/models/menkaburikozo.glb`を書き出し)。
>
> **骨格は計画書どおり`tsubute`と同じ関節構成(hip/chest/head/armF/
> handF/kneeB/ankleB/footB)をベースにした。** 未決事項にあった
> 「アーマチュアの流用可否」は、座標を全体的に低く潰すだけで問題なく
> 流用できることを確認した。`tsubute`のずんぐりした立体感を潰し、
> 高さをおよそ半分にすることで「隣接するまで気配を消す」(ambush AI)
> にふさわしい、平たく低いシルエットにした。
>
> **正面に祭り面を貼り付けて由来を表した。** `uv_sphere`を扁平に潰した
> 円盤2枚(くすんだ紅色の面本体+金色の縁取り)を頭の正面に重ね、
> 目の穴だけ暗い色の小さな`uv_sphere`で落とし込むことで、「出し物の陰に
> 潜む」不気味さと祭りの名残を両立させた。
>
> **配色は計画書どおり、くすんだ紅色・金色の名残(面)と、目立たない
> 暗色(体・くすんだ焦茶の布)。**
>
> **三角形数は4,032**(既存モデルの目安1,800〜7,500の範囲内)。
>
> **アニメーションは`tsubute_animations()`を流用せず、新規にキーフレーム
> を書いた。** 石を投げる`tsubute`とは異なり、`idle`はほとんど動かず
> 気配を消す静止に、`attack`は面を突き出すように跳びかかる不意打ちの
> 動きにした(クリップ構成・ボーン名は変えていない)。
>
> **見た目の確認は`tools/preview/menkaburikozo.png`のCyclesレンダーで
> 行った。** 裏返り・体積ゼロ・パーツの誤結合といった破綻はない。
> `tools/compare_models.mjs`で全モデル(41個)を再ビルドして構造(頂点数・
> クリップ名・外形)を突き合わせ、一致することも確認した。
>
> **`src/entities/species.ts`の`menkaburikozo.model`を`"tsubute"`→
> `"menkaburikozo"`に変更した。** `menkaburikozo`は
> `tests/monster-roster-expansion-species.test.ts`の60種化ロースター拡張
> (`NEW_SPECIES_IDS`)には含まれていない種族のため、`KNOWN_MODEL_IDS`の
> 更新は不要だった。
>
> `npx tsc --noEmit`・`npx vitest run`(1153/1153)・`npm run build`は
> すべてgreen。未決事項だった「最終的な三角形数・関節配置の微調整」は
> 上記の数値で確定させた。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。

# めんかぶりこぞう(menkaburikozo)の3Dモデル

現在は`tsubute`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

出し物の陰に潜む悪戯。隣接するまで気配を消し、不意打ちが決まると相手を混乱させる。

AI挙動(`ambush`)に合わせ、周囲に溶け込む、平たく低いシルエット。目立たない配色にする。
配色は第7地方(わすれられた祭りの跡)のテーマに合わせ、
褪せた祭りを思わせる、くすんだ紅色・金色の名残を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`tsubute`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`tsubute`モデルを基準に、ステータス(maxHp 42)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`tsubute`のアーマチュアをそのまま流用できるか、一部調整が要るか
