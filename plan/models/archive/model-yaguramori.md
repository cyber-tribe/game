> **実装済み。** `tools/models/monsters.py`に`build_yaguramori()`/
> `yaguramori_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py yaguramori`で
> `public/models/yaguramori.glb`を書き出し)。
>
> **骨格は計画書どおり`madoromi`と同じ関節構成(root/stem/capbase/
> captop)をベースにした。** 未決事項にあった「アーマチュアの流用
> 可否」は、関節名・ボーン構成をそのまま使い、座標・太さだけを差し
> 替える形で問題なく流用できることを確認した。きのこの傘ではなく
> 祭りの櫓を思わせる姿にするため、`stem`を`madoromi`より長く細く
> 伸ばして柱にし、`capbase`→`captop`の高さを大きく詰めて平たい
> 屋根板にした。
>
> **「矢のような一撃」を放つ由来にちなみ、屋根の中心に鏃形の棘を
> 立てた。** `cone()`はZ軸沿いにしか作れないため回転はかけず、
> 中心に大きな鏃1本、周囲に小さな飾り棘3本を配置した。目・口は
> `madoromi`と同様に軒下(屋根の裏側)に潜ませ、覗き見るような表情に
> した。
>
> **最初のビルドは三角形数1,376で、既存モデルの目安(1,800〜7,500)を
> 下回った。** `build_skinned`の`subsurf`を2→3に上げることで3,104まで
> 増やし、目安の範囲に収めた(造形そのものは変えていない)。
>
> **配色は計画書どおり、くすんだ紅色・金色の名残(屋根・棘)と、
> 古びた柱の木色。**
>
> **アニメーションは`madoromi_animations()`を流用せず、新規に
> キーフレームを書いた。** `idle`は櫓の上でじっと見下ろすほとんど
> 静止した待機、`attack`は屋根を大きく傾けて鏃の棘を狙いに合わせてから
> 放つ動きにした(クリップ構成・ボーン名は変えていない)。
>
> **見た目の確認は`tools/preview/yaguramori.png`のCyclesレンダーで
> 行った。** 裏返り・体積ゼロ・パーツの誤結合といった破綻はない。
> `tools/compare_models.mjs`で全モデル(47個)を再ビルドして構造(頂点数・
> クリップ名・外形)を突き合わせ、一致することも確認した。
>
> **`src/entities/species.ts`の`yaguramori.model`を`"madoromi"`→
> `"yaguramori"`に変更した。** `yaguramori`は60種化ロースター拡張
> (`plan/game/archive/monster-roster-expansion-species.md`)由来の種族でも
> あるため、`tests/monster-roster-expansion-species.test.ts`の
> `KNOWN_MODEL_IDS`に`yaguramori`を追記し、直前のコメントも更新した。
>
> `npx tsc --noEmit`・`npx vitest run`(1165/1165)・`npm run build`は
> すべてgreen。未決事項だった「最終的な三角形数・関節配置の微調整」は
> 上記の数値で確定させた。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。
>
> **これで第七地方(わすれられた祭りの跡)は、地方ボス(見世物のぬし)を
> 除く全種族(めんかぶりこぞう・かざりだるま・かげぼうし・
> ちょうちんおくり・わたあめのおばけ・やぐらもり)の専用モデルが揃った。**

# やぐらもり(yaguramori)の3Dモデル

現在は`madoromi`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

祭りの櫓に住み着いた古い霊。見世物のぬしの手先として、高い場所から矢のような一撃を放つ。

AI挙動(`ranged`)に合わせ、何かを飛ばす・放つための器官(口・触手・棘)を強調した形にする。
配色は第7地方(わすれられた祭りの跡)のテーマに合わせ、
褪せた祭りを思わせる、くすんだ紅色・金色の名残を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`madoromi`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`madoromi`モデルを基準に、ステータス(maxHp 36)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`madoromi`のアーマチュアをそのまま流用できるか、一部調整が要るか
