> **実装済み。** `tools/models/monsters.py`に`build_yumemirupurun()`/
> `yumemirupurun_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py yumemirupurun`で
> `public/models/yumemirupurun.glb`を書き出し、`src/entities/species.ts`の
> `yumemirupurun.model`を`"purun"`から`"yumemirupurun"`に変更)。
>
> **骨格は計画書どおり`purun`と同じ縦2本(base-mid-top)の関節構成を
> そのまま流用した。** 未決事項にあった「アーマチュアの流用可否」は、
> 座標をおよそ1.12倍にするだけで問題なく流用できることを確認した
> (`tokoshiepurun`の1.15倍・`subetenopurun`の1.2倍に近い前例どおり)。
>
> **「攻撃に眠りを乗せる」由来を、同じ`purun`系統の兄弟種
> (`tokoshiepurun`・`subetenopurun`)とは異なる手法で表現した。**
> 両種の目は`eyeball()`ヘルパーの`squash`引数で白目を潰すだけだったが、
> 本種は`squash=0.62`に加えて、白目の上から覆いかぶさる専用の
> 「まぶた」ジオメトリ(平たく潰した球)を別途重ねている。さらに
> 頭上に、ほのかに発光する小さな球を3つ「夢の粒」として浮かべ、
> 見ただけで眠気を誘うような気配を足した(`kinokootoko`の胞子と
> 似た「発光する小球を添える」技法自体は既存だが、体表に貼り付けず
> 頭上に離して浮かせる点、色を胞子の黄ではなく夢めいた淡紫にした点で
> 用途を変えている)。`idle`アニメーションも、舟をこぐように深く
> 傾いては、はっと我に返って起き直る「船を漕ぐ」動きにして、
> まどろみと目覚めを繰り返す性質を動きでも示した。
>
> **配色は計画書どおり、第一地方(うたたねの参道)の土色に馴染む
> 淡い色合いを基調に、マドロミダケ由来の紫みをわずかに混ぜた**
> (`purun`本来の青、`tokoshiepurun`の素の土色タンとも異なる、
> 灰みがかった薄紫系のくすみ色)。
>
> **睡眠付与の発動率について:** 本文書には具体的な数値の記載は
> ないが、関連文書(`plan/game/archive/monster-yumemirupurun.md`)には
> 「sleep25%」という記述があった。一方、`src/entities/species.ts`の
> 実装は`inflicts: { kind: "sleep", chance: 0.2, turns: 3 }`(20%)で
> あり、既に別PR(#332)でこの食い違いが確認され、実装済みコードの
> 20%を信頼できる値として扱う方針が決まっている。本モデル実装では
> ステータス・発動率のいずれも変更していない(見た目のみの追加)ため、
> この食い違いに影響はない。
>
> **三角形数は2,704**(既存モデルの目安1,800〜7,500の範囲内)。
>
> **検証:** `ast.parse`によるモンスター定義の構文チェック、
> `tools/build_models.py --no-preview`による全登録モンスターの
> 再ビルド(本種以外に変更が無いことを確認)、プレビュー画像
> (`tools/preview/yumemirupurun.png`)の目視確認(法線の反転・
> 体積ゼロの部位・パーツの誤結合がないこと)、`npx tsc --noEmit`・
> `npx vitest run`・`npm run build`がいずれも成功することを確認した。

# ゆめみるぷるん(yumemirupurun)の3Dモデル

現在は`purun`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

まどろみの余韻に眠気そのものが混ざり込み、攻撃に眠りを乗せるようになった姿。同じぷるんから出発しても、寄り添う相手しだいで別の夢に育つ。

AI挙動(`melee`)に合わせ、がっしりした体格で、正面から迫る力強いシルエットにする。
配色は第1地方(うたたねの参道)のテーマに合わせ、
参道の土色に馴染む、素朴な淡い色合いを基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`purun`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`purun`モデルを基準に、ステータス(maxHp 20)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`purun`のアーマチュアをそのまま流用できるか、一部調整が要るか
