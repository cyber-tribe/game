> **実装済み。** `tools/models/monsters.py`に`build_houshitobi()`/
> `houshitobi_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py houshitobi`で
> `public/models/houshitobi.glb`と`tools/preview/houshitobi.png`を書き出し)。
> `src/entities/species.ts`の`houshitobi`エントリの`model`を`"madoromi"`から
> `"houshitobi"`に切り替えた。
>
> **骨格は計画書どおり、現在流用中の`madoromi`と同じ「幹1本+傘」の
> 関節の"種類"を土台にした。** 未決事項だった「`madoromi`のアーマチュアを
> そのまま流用できるか」への回答は「関節の種類(root-stem-capbaseの縦の芯+
> captop)は流用できるが、遠隔攻撃を担う個体としては新規の関節が要る」
> というもの。傘の先から前方・斜め上へ突き出す噴出口(`spout`)と、
> 傘の後方から垂れる触手状の付属肢(`tendril`/`tendriltip`、左右対称)を
> 追加し、計9関節・8ボーンにした。
>
> **最初の試作では、噴出口(`spout`)を傘の中心からY方向に0.34と長く・
> ほぼ水平に伸ばしすぎ、幹・傘・噴出口が一本につながった「バナナ状の塊」
> にしか見えず、マドロミダケらしい傘のシルエットが消えてしまった。**
> 傘の半径を0.185→0.215に太らせて張り出しをはっきりさせたうえで、
> 噴出口を0.34→0.27に短縮し、斜め上向きに角度をつけて主張を抑え、
> さらに触手の付け根を胴の途中(帯のように体を横切る位置)から傘の
> 後方(`x=0.22, y=0.15`、垂れ下がる形)へ逃がして、色分けの帯が
> 胴を横切って見える問題も解消した。傘・噴出口・触手はいずれも、
> 付け根の関節(`capbase`)の半径(0.215)をはっきり超える距離に置き、
> 皮に呑み込まれて見えなくなる事故を避けている。
>
> **配色は計画書どおり、湿った土色の幹と、胞子を思わせる淡い黄土色の
> 傘・噴出口・触手の2トーンで塗り分けた。** 噴出口・触手は関節からの
> 距離(`min`距離、nukarumigani/wasuremizuchiと同じ手法)、それ以外は
> 高さ(`z > 0.28`)で判定し、実際の面数(幹384面/胞子色616面)を確認して
> 偏りがないことを検証した。噴出口の先端には暗色の小さな開口部を、
> そこから浮遊するように小さく・淡く発光を強めながら4つの胞子の粒
> (`houshi_mote`)を尾のように飛ばし、「胞子を遠くへ飛ばす」個体である
> ことを視覚的に補強している。目は半分閉じた眠たげな造形にし、
> マドロミダケと同じ「眠りを誘う」系統であることを示した。
>
> 三角形数は3,936(既存モデルの目安1,800〜7,500の範囲内)。
> `npx tsc --noEmit`・`npx vitest run`(1,119件)・`npm run build`は
> いずれも成功。`tests/monster-roster-expansion-species.test.ts`の
> `KNOWN_MODEL_IDS`に`houshitobi`を追記した(既存の並びは変更していない)。
> `npm run dev`+Playwrightで実機のヘッドレス確認も行った。図鑑ギャラリー
> (`plan/gallery-mode.md`)を、`save.compendium.houshitobi = "captured"`を
> 直接セットしたうえで種族idを指定して開くデバッグ的な手順で開き、
> 名前・生態文とともに新モデルが実際にロードされ、コンソールエラーなしで
> 描画されることを確認した。
>
> Blenderでの造形確認(`tools/preview/houshitobi.png`)は、最初の試作
> (バナナ状)を目視で気づいて上記のとおり作り直し、正面・背面・真上からの
> 追加アングルでも傘・触手・噴出口のシルエットが破綻していないことを
> 確かめてから、最終形を採用した。

# ほうしとび(houshitobi)の3Dモデル

現在は`madoromi`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

舞い散る胞子の化身。遠くから胞子を飛ばして眠りを誘う、マドロミダケの遠隔版というべき存在。

AI挙動(`ranged`)に合わせ、何かを飛ばす・放つための器官(口・触手・棘)を強調した形にする。
配色は第3地方(まどろみの茸林)のテーマに合わせ、
茸林らしい、湿った土色と胞子の淡い黄土色を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`madoromi`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`madoromi`モデルを基準に、ステータス(maxHp 20)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`madoromi`のアーマチュアをそのまま流用できるか、一部調整が要るか
