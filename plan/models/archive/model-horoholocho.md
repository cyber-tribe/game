> **実装済み。** `tools/models/monsters.py`に`build_horoholocho()`/
> `horoholocho_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py horoholocho`で
> `public/models/horoholocho.glb`を書き出し)。他のモデルPRと同じく、
> 既に用意済みのvenvとnode_modulesをworktreeにシンボリックリンクして
> 再利用し、再ダウンロードは避けた。
>
> **骨格は計画書どおり、現在流用している`purun`の関節構成(縦2本、
> `base`→`mid`→`top`)をそのまま踏襲した。** `akubitokage`・
> `kodamausagi`に続く「`purun`骨格の再利用」の3例目で、未決事項にあった
> 「`purun`のアーマチュアをそのまま流用できるか」への回答は「関節の
> 座標・太さだけを新規に定義すれば、ボーン名・数ともに完全に流用できる」
> というもの。底面を床で潰す処理もそのまま使え、アニメーションの
> ボーン名が`purun`と同一になるため、`horoholocho_animations()`は
> `purun_animations()`を呼ぶだけで済んだ(`kodamausagi`と同じ書き方)。
> これにより計画書の「新規モーション制作は増やさない」を、コード上も
> 実質的な重複なしに満たしている。
>
> **シルエットは、`swarm`(群れサイズ3〜4)で複数体まとめて描画される
> 前提のため、脚を生やさず`purun`と同じ丸い雫形のままにとどめた。**
> `top`の関節をわずかに後方へ反らせて`akubitokage`と似た「傾き」を
> 与えつつ、あちらより起伏を穏やかにして、羽毛の房がまどろみながら
> 傾いているように見せている。装飾は目と、胴の両脇に低く寝かせた
> 楕円形の翼(`uv_sphere`を`scale=(0.30, 1.25, 0.55)`で押しつぶした
> もの)だけにとどめ、`ashiatodori`や`mabutamushi`のような脚・尾・
> くちばしは足していない。目は`eyeball()`ヘルパーに`squash=0.45`を
> 渡して縦につぶし、まぶたが落ちかけた眠たげな表情にした。
>
> 試作の初期段階では胴の色分けをおおむね半々にしていたが、プレビュー
> レンダーで見ると胞子色が画面の大半を占めてしまい「湿った土色を基調に
> 胞子の淡い黄土色」という計画書の配色方針(土色が主、胞子色は従)と
> ずれていたため、閾値を体の上端付近まで引き上げ、頭頂だけが淡い
> 胞子色の粉をかぶったような塗り分けに直した。あわせて翼の色も
> 胞子色寄りの黄土色から、土に馴染む焦茶`(0.42, 0.32, 0.22)`へ変更した。
> この調整の過程で、変更前は`glTF`書き出し時に`ashiatodori`と同様の
> 「Mesh is not valid」警告(翼の`uv_sphere`スケールに起因するもの、
> 既存モデルにも見られる無害な警告)が出ていたが、色分け閾値と翼の
> スケールを調整した最終版では警告自体が出なくなったことも確認した。
>
> **配色は第3地方(まどろみの茸林)のテーマどおり、湿った土色
> `(0.30, 0.21, 0.14)`を基調に、頭頂の胞子色`(0.84, 0.75, 0.48)`を
> `kinokootoko`・`houshitobi`と同系統の淡い黄土色にそろえた。** 目の
> 白目もわずかに暖色のクリーム`(0.92, 0.88, 0.76)`にして、「夢」らしい
> 柔らかさを出している。
>
> **三角形数は2,032**で、既存モデルの目安(1,800〜7,500)の範囲内、
> かつ`purun`(1,780)に近い軽さを保っている。`swarm`で複数体まとめて
> 描画される負荷を、計画書の方針どおり単体の造形を切り詰めることで
> 抑えた。
>
> **見た目の確認は`tools/preview/horoholocho.png`のCyclesレンダーに加え、
> `render_preview()`をyaw違いで複数回呼ぶ臨時スクリプトで正面・背面・
> 左右斜めからのレンダーも確認した。** 裏返り(法線反転)・体積ゼロの
> パーツ・パーツの誤結合といった破綻はなく、`purun`系の底面フラット化
> 処理も既存モデルと同じく無害な no-op(この関節配置では床より下に
> 頂点が出ないため)であることを検証スクリプトで確かめた。
>
> **`src/entities/species.ts`の`horoholocho.model`を`"purun"`→
> `"horoholocho"`に変更した。** `src/modelList.ts`はSPECIES/ITEMSから
> 動的にモデル一覧を組み立てる作りのため、明示的な追加登録は不要だった。
>
> **`tests/monster-roster-expansion-species.test.ts`は変更していない。**
> `horoholocho`は同ファイルの`NEW_SPECIES_IDS`(ロースター拡張PRで追加された
> 30種)には含まれておらず、`KNOWN_MODEL_IDS`もその30種の`model`検証専用
> なので、対象外と判断した。`tests/monster-compendium.test.ts`・
> `tests/companion-evolution-expansion.test.ts`は`horoholocho`をspecies
> id/配合素材idとして参照しているだけで`model`フィールドは見ていないため、
> 変更不要だった。
>
> **検証は`tools/build_models.py --no-preview`によるモデル全件の再ビルド
> (`horoholocho`以外に破損・意図しない変更がないことを確認、非決定的な
> 差分が出た`.glb`は`git checkout`で元に戻した)、`ast.parse`によるシンタックス
> チェック、`npx tsc --noEmit`、`npx vitest run`(1229/1229 green)、
> `npm run build`をすべて実施し、いずれも問題なし。**
>
> **未決事項だった「最終的な三角形数・関節配置の微調整」は上記の数値・
> 座標で確定させた。** 「既存`purun`のアーマチュアをそのまま流用できる
> か」も、関節座標・太さのみ新規定義すればボーン構成ごと流用できる、
> という結論で解消した。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。

# ホロホロチョウ(horoholocho)の3Dモデル

現在は`purun`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

ちぎれた微睡みの欠片。1羽ずつは非力だが、微睡みが積み重なるように群れで現れる。

AI挙動(`swarm`)に合わせ、単体は簡略化した小さなシルエット(複数体まとめて配置される前提)にする。
配色は第3地方(まどろみの茸林)のテーマに合わせ、
茸林らしい、湿った土色と胞子の淡い黄土色を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`purun`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`purun`モデルを基準に、ステータス(maxHp 14)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`purun`のアーマチュアをそのまま流用できるか、一部調整が要るか
