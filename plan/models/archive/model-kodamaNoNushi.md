> **実装済み。** `tools/models/monsters.py`に`build_kodamaNoNushi()`/
> `kodamaNoNushi_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py kodamaNoNushi`で
> `public/models/kodamaNoNushi.glb`を書き出し)。
>
> **骨格は計画書どおり`gajiri`と同じ関節構成をベースにした。**
> 未決事項にあった「アーマチュアの流用可否」は、座標のスケールを
> ボスサイズまで拡大するだけで問題なく流用できることを確認した。
>
> **「尾根じゅうに響いてきた無数のこだまが、ひとつに重なり合って生まれた
> 姿」という由来から、地方の他の種族の特徴を1体に集約する構成にした。**
> `kodamausagi`譲りの長い耳、`kodamagumo`譲りの雲状の膨らみ(`uv_sphere`
> 4個を背に重ねる)、`kaerukodama`譲りの大きく見開いた目、を組み合わせて
> 「地方の主として響き続ける」姿にした。膨らみは前作の`nedayamabiko`で
> 頭部と混ざって見分けが付かなくなった反省を踏まえ、頭より低く・背中側
> にはっきり離して配置している。
>
> **配色は計画書どおり岩肌の灰色と乾いた土色の2トーン。**
>
> **三角形数は7,084**(既存モデルの目安1,800〜7,500の範囲内、上限に近い
> がボスとして許容範囲とした)。
>
> **アニメーションは`gajiri_animations()`を流用せず、新規にキーフレーム
> を書いた。** クリップ構成(idle/walk/attack/hit/die、ボーン名も
> `chest-neck`等gajiri由来のまま)は変えていないが、地方の主らしい
> ゆったり大きな待機の揺れと、重く力強い足取り・一撃にした。
>
> **見た目の確認は`tools/preview/kodamaNoNushi.png`のCyclesレンダーで
> 行った。** 裏返り・体積ゼロ・パーツの誤結合といった破綻はない。
> `tools/compare_models.mjs`で全モデル(40個)を再ビルドして構造(頂点数・
> クリップ名・外形)を突き合わせ、一致することも確認した。
>
> **`src/entities/species.ts`の`kodamaNoNushi.model`を`"gajiri"`→
> `"kodamaNoNushi"`に変更した。** `kodamaNoNushi`は地方ボス
> (`minFloor: Infinity`・`weight: 0`)のため
> `tests/monster-roster-expansion-species.test.ts`の60種化ロースター拡張
> (`NEW_SPECIES_IDS`)には含まれておらず、`KNOWN_MODEL_IDS`の更新は
> 不要だった。
>
> `npx tsc --noEmit`・`npx vitest run`(1151/1151)・`npm run build`は
> すべてgreen。未決事項だった「最終的な三角形数・関節配置の微調整」は
> 上記の数値で確定させた。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。
>
> **これで第六地方(こだまの尾根)の全種族(こだまうさぎ・こだまぐも・
> かえるこだま・やまびこおに・ねだやまびこ・やまびこぎつね・
> こだまぎつね・こだまの主)の専用モデルが揃った。**

# こだまの主(kodamaNoNushi)の3Dモデル

現在は`gajiri`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

尾根じゅうに響いてきた無数のこだまが、ひとつに重なり合って生まれた姿。地方の主として、絶えず響き続ける尾根の中心に立つ。

AI挙動(`melee`)に合わせ、がっしりした体格で、正面から迫る力強いシルエットにする。
配色は第6地方(こだまの尾根)のテーマに合わせ、
吹きさらしの尾根らしい、岩肌の灰色と乾いた土色を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`gajiri`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`gajiri`モデルを基準に、ステータス(maxHp 76)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`gajiri`のアーマチュアをそのまま流用できるか、一部調整が要るか
