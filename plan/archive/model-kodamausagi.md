> **実装済み。** `tools/models/monsters.py`に`build_kodamausagi()`/
> `kodamausagi_animations()`を追加し、`MONSTERS`に登録した
> (`tools/venv/bin/python tools/build_models.py kodamausagi`で
> `public/models/kodamausagi.glb`を書き出し)。`bpy`は
> `PYTHON=/usr/bin/python3.11 bash tools/setup_blender.sh`で問題なく
> 用意できた。
>
> **骨格は計画書どおり`purun`の縦2本(`base-mid-top`)をそのまま流用した。**
> 関節名・ボーン構成は完全に同じまま、座標と太さだけを差し替えている
> (`purun`の底半径0.29に対し`base`半径0.195・高さは0.34。群れで配置される
> 前提のため、`purun`よりひとまわり詰まった丸い胴にした)。未決事項に
> あった「アーマチュアの流用可否」は、この座標差し替えだけで問題なく
> 流用できることを確認した。
>
> **「こだまうさぎ」と分かる特徴として、耳だけを新規パーツで足した。**
> `cone()`はZ軸沿いにしか作れない(過去の反省からこのAPIに回転引数は無い)
> ため、根元(半径大)を頭の高さに置いて真上へ伸ばすだけの配置にし、
> 左右2本を寄せて細く長い耳のシルエットにした。目・口は`purun`と共通の
> `eyeball()`ヘルパー等をそのまま再利用している。
>
> **配色は計画書どおり岩肌の灰色と乾いた土色の2トーン。**
> `assign_materials_by_region`で高さにより塗り分け、頭側を土色
> `(0.62, 0.50, 0.36)`、腹側を岩の灰色`(0.52, 0.50, 0.47)`にした
> (耳は土色寄りの`(0.66, 0.54, 0.40)`で頭と揃えた)。
>
> **三角形数は1,852**(`purun`と同じSkin+サブディビジョンlevel=2構成のため
> `purun`の1,780に近い数字に収まった)。既存モデルの目安(1,800〜7,500)の
> 範囲内。書き出しサイズは67.6KB。
>
> **アニメーションは計画書どおり`purun_animations()`をそのまま呼び出すだけ**
> にした(ボーン名が`purun`と同じ`base-mid`/`mid-top`のため、新規に
> キーフレームを作らずに流用できる。5クリップとも既存の枠のまま)。
>
> **見た目の確認は`tools/preview/kodamausagi.png`のCyclesレンダーで行った。**
> 裏返り・体積ゼロ・パーツの誤結合といった破綻はない。
> `tools/compare_models.mjs`で全モデルを再ビルドして構造(頂点数・
> クリップ名・外形)を突き合わせ、一致することも確認した。
>
> **`src/entities/species.ts`の`kodamausagi.model`を`"purun"`→
> `"kodamausagi"`に変更した。** `src/modelList.ts`は`SPECIES`から動的に
> 一覧を組み立てる作りのため、明示的な追加登録は不要だった。
>
> `npx tsc --noEmit`・`npx vitest run`(1123/1123)・`npm run build`は
> すべてgreen。未決事項だった「最終的な三角形数・関節配置の微調整」は
> 上記の数値で確定させた。対象外の項目(装備の重ね表現)は計画書どおり
> 手を付けていない。

# こだまうさぎ(kodamausagi)の3Dモデル

現在は`purun`モデルを流用している(仮、`src/entities/species.ts`参照)。
`design/characters.md`の生態設定に基づき、専用モデルを新規に作る。

## 由来と見た目の方針

繰り返す木霊。1匹の声が響きあってこだまするように、群れで現れる。

AI挙動(`swarm`)に合わせ、単体は簡略化した小さなシルエット(複数体まとめて配置される前提)にする。
配色は第6地方(こだまの尾根)のテーマに合わせ、
吹きさらしの尾根らしい、岩肌の灰色と乾いた土色を基調にする。

## 造形の方針

- 元にする骨格: 現在流用している`purun`と同じ関節構成をベースに、
  Skinモディファイア+サブディビジョンで作る(README記載の制作パイプライン)。
- 三角形数の目安: 既存モデル(1,800〜7,500程度)の範囲に収める。
- サイズ: 既存の`purun`モデルを基準に、ステータス(maxHp 22)に
  見合う大きさに調整する。

## アニメーション

既存5クリップ(待機・歩行・攻撃・被弾・消滅)の構成をそのまま流用する。
新規モーション制作は増やさない(アーマチュア+自動ウェイト→キーフレームの
既存パイプラインをそのまま適用する)。

## 対象外

- 造形の最終決定(実装時に`tools/models/`のスクリプトで確定させる)
- `plan/equipped-weapon-visual.md`のような装備の重ね表現(このモンスターには該当しない)

## 未決事項

- 最終的な三角形数・関節配置の微調整
- 既存`purun`のアーマチュアをそのまま流用できるか、一部調整が要るか
