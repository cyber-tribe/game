# 攻撃専用ボタンの導入と、体当たりの「押し出し」化

> **実装済み。**
>
> **コアロジック(`src/game.ts`)**: `Command`に`{ type: "attack" }`を追加し、
> `resolvePlayerCommand`に専用のcaseを設けた(`player.facing`の方向へ
> `resolvePlayerAttack`を呼ぶだけで、必ずターンを消費する=空振りも1手)。
> 一方`movePlayer`側は、進んだ先に敵がいたときの分岐を`resolvePlayerAttack`
> 呼び出しから新設の`pushMonster`呼び出しに差し替えた。`pushMonster`は
> 押し出し先の判定にモンスターAIの移動判定と同じ`canStep`(壁・他アクター・
> タルをまとめて弾く)をそのまま使い、実際の移動は`moveActor`(モンスターの
> 通常移動と同じ経路)に委ねている。これにより、押し出された先が奔流タイル
> 等であっても既存の`applyTorrentPush`が自動で乗る(「そのマスに元々あった
> 効果がそのまま発動する」という計画書の対象外節どおり、専用の追加効果は
> 実装していない)。ダメージが発生する経路は`resolvePlayerAttack`一本に
> 絞られ、通常攻撃・不意打ち・武器パターン等の既存ロジックは無改修。
>
> **キー割り当て(未決事項1)**: `KeyX`を選んだ。`src/view/input.ts`の
> 既存`ACTION_KEYS`を洗い出したところ空いており、WASD移動クラスタの
> すぐ下(左手の小指で届く位置)にあって「移動しながら攻撃に手を伸ばす」
> 操作感と相性がよいこと、他の多くのアクションローグライク/ハクスラで
> 「その場攻撃」に割り当てられる定番キーであることを理由にした。
> `export const ATTACK_KEY_CODE = "KeyX"`として`input.ts`からexportし、
> `main.ts`(デバッグ用ヘルパー)・`touch-controls.ts`から参照している
> (`tools/playtest.mjs`だけはNode側の別プロセスなのでリテラル文字列で
> 二重管理。コメントで参照元を明記した)。
>
> **押し出し失敗時のターン消費(未決事項2)**: 「消費しない」を選んだ。
> `src/game.ts`の`movePlayer`冒頭、壁への体当たり
> (`if (!walkableAt(this.floor, to)) { events.push({type:"bump",...}); return false; }`)
> が既に「バンプはターンを消費しない」という規約になっており、押し出し
> 失敗は見た目にも壁バンプと区別がつかない(何も起きない)ため、これに
> 揃えるのが最も一貫性が高いと判断した。`pushMonster`が`canStep`に
> 失敗した場合も同じ`bump`イベントを出し`false`を返す。
>
> **振り向きながらの攻撃(未決事項3)**: 計画書の想定どおり「向いている
> 方向にそのまま攻撃するだけ」とし、特別な合成入力は実装していない。
> 向きを変えてから攻撃したい場合は、既存のShift+方向(`face`、ターン
> 消費なし)→攻撃キー、の2手順で行う(`tests/`内の変換もすべてこの形)。
> 混乱状態(`STATUS_CONFUSE`)によるふらつきは移動コマンドにのみ適用され、
> 攻撃コマンドの方向には影響しない(向いている方向は`face`で既に確定して
> いるため、攻撃自体をふらつかせる必然性が薄いと判断した簡略化)。
>
> **echoing-ridge(第六地方)ギミックとの関係**: 攻撃で周囲のモンスターに
> 気づかれる`alertNearbyMonsters`は、実際にダメージ判定を含む攻撃
> (`resolvePlayerAttack`経由)でのみ呼ばれる。押し出しは`pushMonster`が
> 独自に処理しており、`alertNearbyMonsters`を呼んでいない(押し出しは
> 「攻撃」ではない、という計画書の切り分けをそのまま踏襲した)。同様に、
> `attack()`内の「攻撃してきた相手には気づく」(`target.aware = true`)も
> 実際に攻撃を受けた場合にのみ発生し、押し出されただけのモンスターは
> awareにならない。
>
> **既存テストの移行**: `move`コマンドで敵に体当たりしてダメージ・撃破・
> 会心などを検証していたテストを、`tests/`配下で機械的に洗い出し、
> 影響のある箇所だけ`face`(必要な場合)+`attack`の2コマンドに置き換えた。
> 変更したファイル: `tests/combat-mechanics.test.ts`・
> `tests/dual-mark-equipment.test.ts`・`tests/equipment-forging.test.ts`・
> `tests/echoing-ridge.test.ts`(4件中3件。罠のテストは対象外)・
> `tests/game.test.ts`・`tests/protagonist-training.test.ts`(3件)・
> `tests/region-boss-misemonononushi.test.ts`・
> `tests/region-boss-nushigaeru.test.ts`・`tests/tutorial.test.ts`
> (5件中2件。トラップ系は対象外)・`tests/weapons.test.ts`(9件中7件)。
> `tests/barrel.test.ts`・`tests/item-catalog-expansion.test.ts`・
> `tests/lost-and-found-vault.test.ts`・`tests/monster-house.test.ts`・
> `tests/monster-roster-expansion-species.test.ts`・
> `tests/region-expansion.test.ts`・`tests/shops-and-thieves.test.ts`・
> `tests/stance.test.ts`・`tests/tarukurabe.test.ts`・
> `tests/waterfall-torrent.test.ts`は、`move`を含んではいるものの実際には
> 壁・タル・仲間・杖・AI単体呼び出し等、本変更と無関係な検証だったため
> 変更していない(1件ずつ内容を確認済み)。デフォルトの向き(`facing: 4`
> =南)がテストの想定方向と食い違う箇所は、変換時に明示的な`face`
> コマンドを追加している(既存の`move dir:0`が"向き+ダメージ"を1コマンド
> で兼ねていたぶん、隠れていた前提だった)。
>
> 専用の新規テスト`tests/attack-button.test.ts`を追加し、押し出しの成立・
> 壁ふさがりでの押し出し失敗(ターン不消費)・別アクターでの押し出し失敗・
> 攻撃キーの命中・空振り(ターン消費のみ)・仲間AIが無改修であることを
> それぞれ検証している。押し出しが成立した同じ`command()`呼び出し内で、
> 押し出されたモンスターがそのまま自分のAIの1手(プレイヤーへの追跡)を
> 消化する(計画書の「押し出された敵も次のターンには通常通りプレイヤーへ
> 向かってくる」を字面どおり読むと"次のターン"だが、実装上は同一ターンの
> `runActors`で即座に反応する)ことをテスト作成時に確認したため、押し出し
> 直後の位置は最終状態ではなく最初の`move`イベントで検証している。
>
> **タッチ操作(`src/ui/touch-controls.ts`・`index.html`)**:
> `plan/archive/touch-controls.md`が実装時点で見送っていた攻撃ボタンを
> 追加した。常時表示のアクションボタン群(タル/投げる/道具/決定)に
> 「攻撃」ボタンを加え、`ATTACK_KEY_CODE`をそのままtapで発行する。
> 5個になったぶん`index.html`側のグリッドを2列→3列に詰め直した
> (ボタン一辺は58px→52pxに縮小。実機での調整前提の暫定値である点は
> `touch-controls.md`の既存注記を踏襲する)。仮想パッドの移動説明も
> 「押し出し」に修正した。なお`plan/archive/touch-controls.md`自身の
> 実装注記(「攻撃ボタンは設けていない」)は当時の記録として書き換えて
> いない(このファイルの本注記が最新の状態を示す)。
>
> **README・チュートリアル**: 操作表の該当行を「1マス押し出す」に修正し、
> `X`キーの行を追加した。`src/entities/settings.ts`の`KEY_REFERENCE`
> (設定画面・Hキーのヘルプオーバーレイの出典)も同様に更新。
> `src/core/tutorial.ts`の初回チュートリアルヒント`moveAndAttack`の文面も
> 「1マス押し出せる」「Xキーで攻撃」に書き換えた(IDはセーブ互換のため
> 変更していない)。
>
> **検証**: `npx tsc --noEmit`・`npx vitest run`(92ファイル/1075件、
> 全green)・`npm run build`をいずれも確認。`tools/playtest.mjs`も更新し
> (殴り合い・タルでの弱らせの両方を攻撃キーのtapに置き換え)、ローカルの
> `npm run dev`に対して実際に実行し、コンソールエラー0件・全工程成功を
> 確認済み。

`plan/touch-controls.md` 策定時、現在の操作(README「モンスターがいる
方向へ進むと攻撃」)について、プレイヤーから「移動したのか攻撃したのか
分かりにくい」との報告があった。当初は演出強化での解決を提案したが、
**「わざと空振りして敵を1マス動かす、という戦略を取れるようにしたい」**
という具体的な狙いが示されたため、方針を変更し、**攻撃を移動から独立した
専用ボタンにする**。

## 変更の核心: 移動・押し出し・攻撃を分ける

- **移動キーで敵のいる方向へ進もうとすると、これまでの自動攻撃ではなく
  「敵を1マス押し出す」動作になる。** 敵の向こう側のマスが空いていれば
  敵はそちらへ1マス動き、プレイヤーはその場に留まる(壁・別のアクターが
  いて押し出せない場合は、通常の壁への体当たりと同じく何も起きない)。
- **攻撃は新しい専用キーで行う。** プレイヤーが向いている方向(Shift+方向
  で変える、既存の「向き」の概念をそのまま流用する)に対して、装備中の
  武器の攻撃パターン(`WeaponPattern`。直線・扇状 等)で攻撃する。攻撃は
  その場から動かない。
- 攻撃キーを、敵がいない方向に対して押すと**空振り**になる(何も起きない
  が、ターンは消費する)。これが「わざと空振りする」という行動そのものを
  成立させる。

## この設計が開く戦略

- 敵を押し出して、地方固有ギミック(奔流タイル・深みタイル 等)や罠、
  あるいは仲間の攻撃射線に押し込む、という運用ができるようになる。
  `plan/item-catalog-expansion.md` の場所替えの杖・引き寄せの杖(遠隔で
  アクターを動かす)と同じ「位置を操作する」設計思想を、素手の基本操作
  にまで持ち込む形になる。
- 群れ(`swarm`)を1体だけ押し出して各個撃破する、通路をふさぐ`guard`系
  を押しのけて道を作る、といった応用も生まれる。

## キー割り当て

- 新規キー1個を追加する(`src/view/input.ts` の `KeyMap`。空いている
  キーから実装時に選ぶ。README操作表の更新も必要)。
- 既存の移動キー(矢印/WASD/テンキー)の意味は「移動、または押し出し」に
  変わる(自動攻撃ではなくなる)。
- Shift+方向(その場で向きだけ変える)は変更しない。

## `plan/touch-controls.md` への影響

タッチ操作側も本PRであわせて更新する(詳細は同文書側の変更を参照)。
移動用の仮想パッドの説明を「押し出し」に修正し、常時表示のアクション
ボタン群に「攻撃」ボタンを新設する。

## バランス上の注意

- 押し出し・空振りは、通常の移動・攻撃と同じく1ターンを消費する
  (「わざと空振りする」ことに何のコストも無いと乱用されてしまうため)。
- 敵を押し出して一方的に有利になりすぎないよう、敵AIの追跡ロジック
  (README「距離場」)は変更しない。押し出された敵も次のターンには通常
  通りプレイヤーへ向かってくる。

## 対象外

- 仲間の攻撃・移動の操作方法(仲間はAI操作のままで、プレイヤーの攻撃
  ボタン導入とは無関係。`plan/companion-orders.md` の指示体系は変えない)
- 敵を押し出した結果、罠・地形ギミックに押し込んだ際の追加ダメージ演出
  等の細部(基本は「そのマスに元々あった効果がそのまま発動する」という
  素直な扱いに留め、専用の追加効果は作らない)

## 未決事項

- 具体的な新規キーの割り当て
- 押し出しに失敗した場合(壁際・別アクターがいる等)にもターンを消費
  するか
- プレイヤーが向いていない方向への攻撃(振り向きながらの攻撃)を許可
  するか、向き変更(Shift)と攻撃を同時に行いたい場合の操作
- README操作表・チュートリアル(`plan/tutorial.md`)の更新
