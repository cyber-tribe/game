> **実装済み。** `TownScreen`(`src/ui/town.ts`)の各`renderColumn(N, ...)`
> (列の中身)は一切変えず、「開ける列の集合」と「見出し(建物名)」を
> `show()`に渡せるようにしただけ。`src/ui/townCursor.ts`の
> `nextTownColumn(current, direction, openColumns?)`に第3引数
> `openColumns`を追加し、渡すと集合内(昇順)の隣の列だけへ移り、両端では
> 従来どおり留まる(省略時は0〜19を一直線に辿る旧来の挙動と完全互換)。
> `handleKey`内の全37箇所の`nextTownColumn`呼び出しに`this.openColumns`を
> 追加で渡すだけで、列を横断する側の実装は揃った。`render()`側も
> `renderColumnByNumber(n)`という振り分け関数を新設し、
> `this.openColumns`に含まれる列だけをDOMへ並べるよう変えた
> (`renderColumn`自体・各列の中身は無変更)。
>
> **建物・村人と列の対応は計画書の表どおりに実装した。**
> `src/view/village.ts`の`VillageBuilding`に`columns: readonly number[]`
> (開ける列の集合)と`role: string`(ヒントに添える役割の一言)を追加し、
> `VILLAGE_BUILDINGS`の各エントリに設定した。村の広場だけは`columns`に
> 17(宵祭りの出店)を含めたうえで、`main.ts`側で`isYoimatsuri`が偽なら
> 呼び出し時に17を除く(列の中身が非開催日の案内を自前で出す仕組みは
> そのまま活かし、建物滞在中に無駄に立ち寄らせないための絞り込みだけ追加)。
>
> **ねむり小屋は、列4(つれていく仲間)をそのまま共用した。** 計画書は
> 「列4のうち仲間の世話(夢あわせ・改名・逃がす)部分」としていたが、
> 列4のUIは「連れて行く選択」と「仲間の世話」が1つの画面に同居しており、
> `renderColumn`を変えない方針の下では分割できない。実害は無い
> (「同じ列を複数の場所から開けるのは構わない」と計画書にもあるとおり、
> 洞窟の入口・ねむり小屋の双方から列4を開けるだけ)と判断し、そのまま
> 共用にした。
>
> **旅の看板は、0〜19のどの列も持たない特別な場所にした
> (`columns: []`)。** 計画書の「従来の『既定の入口(全列)』の役割は廃止」
> を字面どおり実装すると、みんなの記録(`plan/community-leaderboard.md`、
> 未実装)とチュートリアルの読み返しだけが残るが、チュートリアルの読み返しは
> 列19(設定)のサブビューとして既に存在し、かつ列19自体をシステム系
> として建物から外す方針と衝突する。二重実装を避けるため、旅の看板は
> 「村のみんなの記録は準備中」という掲示と、「チュートリアルの読み返しは
> 『≡』メニューの『設定』から」という案内だけを表示する専用ビュー
> (`renderSignpost()`、Enter/Escapeで村へ戻るだけ)にした。列を持たない
> ぶん、既存のSpace即時出発(`departNow`)は意味を持たないため、この画面と
> 後述のシステム設定メニューでは、Spaceでの出発を止めるフラグ
> (`systemMenuMode`)を新設し、Escapeで閉じる導線に差し替えた。
>
> **システム系の列(14=アクセシビリティ・18=音・19=設定)は、どの建物にも
> 割り当てず、新設の「Mキー」(`ActionKey: "systemMenu"`)で村・ダイブ中の
> どちらからでも開けるようにした。** タッチ操作向けの「≡」メニューにも
> 「設定」ボタンを追加した(`src/ui/touch-controls.ts`)。ダイブ中に開いても
> 現在の潜行を打ち切らないよう、`systemMenuMode`中はSpaceキー・
> 「もぐる」ボタンでの即時出発を無効化し、Escape(またはボタンの
> 「とじる」)で呼び出し元(村を歩いていたなら村、ダイブ中だったならその
> ダイブの続き)へ戻す。`README.md`・`KEY_REFERENCE`/`KEY_REFERENCE_TOUCH`
> (`src/entities/settings.ts`)にもMキー/設定ボタンの説明を追記した。
>
> **新設3件(ねむり小屋・記録の間・ガルドの家)の置き場所**:
> 既存の建物と同じ「小屋の外観だけのホットスポット」(`shape: "hut"`、
> 専用の色のみ)として、村の東西の空きスペースに置いた
> (`ねむり小屋`=(8, 1)・ゲンドの工房の東隣、`記録の間`=(8, -3)・おキヨの
> 図鑑小屋の東隣、`ガルドの家`=(-8, -3)・オトネの依頼板の西隣)。
> **番人となる村人(NPCキャラクター)は追加していない。** 計画書の未決事項
> どおり、既存の建物(倉庫・工房・図鑑小屋・村の発展の受付)がいずれも
> NPCモデルを伴わない「建物に近づいて確定するだけ」の作りだったため、
> それと同じ経済的なパターンに合わせた。人物設定を伴う番人NPCを立てるかは、
> 計画書が明記したとおり別PR(`design/characters.md`/
> `design/village-life.md`側)に委ねる。
>
> **出発の支度(洞窟の入口の7列)は、そのまま1つの建物に残した。**
> 「支度→確認→出発」のような段階分割は行っていない。7列はすでに
> `renderColumn`側で個別の操作にまとまっており(倉庫・持ち込み・出発地点・
> 鍛え方・つれていく仲間・難易度・潜るダンジョン)、村なかを3D空間として
> 実際に歩く導線がある以上、出発直前の一連の準備を1か所で済ませられる
> ほうが実用上好ましいと判断した。将来、列がさらに増えて窮屈になった
> 時点で分割を検討すればよい。
>
> **`tools/playtest.mjs`(CI必須の通しプレイジョブ)・`tools/auto-tester.mjs`
> を新しい導線に追従させた。** 従来「旅の看板に近づいてSpace」で拠点画面
> (列0=倉庫)を開き、そのまま持ち込み→出発していたが、旅の看板は列を
> 持たなくなったため、`debugVillagePos()`/`debugVillageBuildings()`という
> デバッグ用の入口(`src/main.ts`)を新設し、村マップの座標を見ながら
> 「洞窟の入口」まで歩いて入るよう両スクリプトを書き換えた。
> `auto-tester.mjs`のランダム探索での回復手順にも、Spaceでは閉じない
> 旅の看板・システム設定メニューに備えてEscapeを先に試す一手を足した。
>
> **確認したこと**: `npx tsc --noEmit`・`npx vitest run`(1305/1305、
> `tests/village.test.ts`・`tests/town-cursor.test.ts`に追加した
> 建物→列対応/巡回範囲のテストを含む)・`npm run build`はすべてgreen。
> ヘッドレスChromium(`node tools/playtest.mjs`、本番ビルドの
> `vite preview`に対して)を複数回実行し、通しプレイが最後まで壊れずに
> 通ることを確認した(1回だけ、既知のflaky signature「仲間にできなかった」
> が発生したが、リソース競合下でのみ再現し、資源を空けたクリーンな環境では
> 再現しなかったため、本変更由来ではないと判断した)。加えて専用の
> Playwright検証スクリプトで、モグラ婆の倉庫(列0・1のみ)・ゲンドの工房
> (列5のみ)・新設3件それぞれに実際に歩いて入り、開く列数・見出し・
> 左右キーでの巡回範囲(集合の外へ出ないこと)を確認し、Mキーで
> システム設定メニュー(列14・18・19)が村なかから開けること、そこでは
> Spaceが効かずEscapeで村へ戻れること、コンソール/ページエラーが
> 一切無いことを確認した。

# 村のメニューを建物・村人ごとの役割に分ける(一元メニューの廃止)

## 経緯

拠点画面(`src/ui/town.ts` の `TownScreen`)は現在、0〜19の20列が
一直線に並ぶ一元メニューで、どの建物から入っても左右キーで全列を
横断できる(`plan/game/archive/town-3d-exploration.md` の
「どの建物から入っても実質すべての機能へたどり着ける」という設計)。

この一元化をやめる。**建物・村人ごとに役割を設け、アクセスすると
その役割に対応する操作だけができる**ようにする。狙い:

- 20列の横断は一覧性が悪く、特にスマホでは横スクロールの海になる
  (`plan/game/archive/gallery-mode.md` の実装注記でも列数増による
  表示崩れが起きていた)。役割ごとに数列ずつなら画面に収まる。
- 「倉庫はモグラ婆」「強化はゲンド」のように**場所と機能が結びつく**
  ことで、村が単なるメニューの入れ物ではなく生活の場になる
  (`design/village-life.md` の「NPCごとに立ち寄れる場所として
  ゾーニングする」方針を、メニュー構造まで貫徹する)。

## 方針

### 実装の骨格

`TownScreen` の各列のレンダラー(`renderColumn(N, ...)`)は
そのまま再利用する。`show()` に**「開ける列の集合」と「見出し
(建物名)」を渡せるようにし、左右キーはその集合の中だけを巡回**
する。全列を一直線に並べる現在の動きを、建物ごとの部分集合に
差し替えるだけで、列の中身(各機能のUI)は変えない。

### 建物・村人と役割の対応

| 場所(村人) | 開ける列 | 役割 |
|---|---|---|
| 洞窟の入口 | 倉庫(0)・持ち込む(1)・出発地点(2)・鍛え方(3)・つれていく仲間(4)・難易度(10)・潜るダンジョン(12) | **出発の支度一式**。ここで整えてそのまま潜る |
| モグラ婆の倉庫 | 倉庫(0)・持ち込む(1) | 持ち物の整理・預け入れ |
| ねむり小屋(新設) | つれていく仲間(4)のうち仲間の世話(夢あわせ・改名・逃がす) | 仲間の管理(`plan/game/archive/monster-fusion.md`) |
| ゲンドの工房 | 工房(5) | 強化・刻印・合成 |
| おキヨの図鑑小屋 | モンスター図鑑(7)・装備図鑑(9) | 図鑑・ギャラリー |
| 記録の間(新設) | 記録の間(6)・実績帳(8) | 記録・実績・称号 |
| オトネの依頼板 | 依頼板(11) | 依頼の受注・報告 |
| 村の発展の受付 | 村の発展(13) | 発展への出資 |
| 村の広場 | NPCと話す(16)・宵祭りの出店(17) | 交流。出店は祭り期間のみ現れる |
| ガルドの家(新設) | 身支度(15) | 衣装の着替え |
| 旅の看板 | みんなの記録・チュートリアルの読み返し | 旅の掲示。従来の「既定の入口(全列)」の役割は廃止 |

- 同じ列を複数の場所から開けるのは構わない(倉庫は入口でも
  モグラ婆でも触れる)。**禁止するのは「そこに無い役割へ左右キーで
  流れていけること」だけ**。
- システム系の列(アクセシビリティ(14)・音(18)・設定(19))は
  建物から外し、**村でもダイブ中でも開ける「≡」メニュー(または
  設定画面)へ移す**。世界の中の場所と、ゲームの外の設定を混ぜない。

### 導線の担保

- 建物に近づいたときのヒント表示(既存の`村なか歩き`の
  「決定ボタン: ◯◯へ」)に、その建物の役割が伝わる一言を添える
  (例:「ゲンドの工房(強化・合成)」)。
- 村は狭く(`VILLAGE_BOUNDS` は約18×18)、建物間の徒歩は数秒
  なので、ファストトラベルは不要。
- 進行条件で列が増える場合(祭り期間の出店等)も、対応する
  建物・場所の集合にだけ追加する。

## 対象外

- 各列の中身(倉庫・工房・図鑑等それぞれのUI・機能)の変更
- 村マップの地形・建物配置の大幅な変更(新設3件の置き場所は
  実装側の裁量。既存の空きスペースに置けばよい)
- ダイブ中のメニュー構造

## 未決事項

- 新設の「ねむり小屋」「記録の間」「ガルドの家」に番人となる村人を
  立てるか(立てる場合の人物設定は `design/characters.md` /
  `design/village-life.md` 側に別PRで追記する)
- 「旅の看板」に掲示する「みんなの記録」(`plan/community-leaderboard.md`)
  の表示位置づけ(未実装機能のため、実装順によっては看板を
  当面チュートリアル掲示のみにする)
- 出発の支度(洞窟の入口)の7列は多いままなので、実装時に
  「支度→確認→出発」のような段階に畳めるかは裁量に委ねる
