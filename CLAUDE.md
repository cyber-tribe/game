# リポジトリ運用ルール

## オリジナリティ

名前・造形・設定を既存タイトルから流用しない。
市販タイトルの固有名詞を md・コード・コミットメッセージに書かない。

## プランニング

成果は md で以下に置く。

| ディレクトリ | 内容 |
|---|---|
| `design/` | 世界観・ストーリー・キャラクター等の設定資料 |
| `plan/game/` | 3Dモデル・音以外のゲーム仕様 |
| `plan/models/` | 3Dモデル1体ごとの仕様 |
| `plan/sound/` | BGM・効果音・音声再生仕様 |

`plan/*/archive/` は実装済み仕様書用。
`plan/*` は実装セッションがそのまま着手できる粒度で書く。

## PR

1. `just fresh <ブランチ名>`
2. 1つの決定・機能だけ編集して `just ship "<メッセージ>"`
   (push・PR作成・オートマージ設定まで行う。完了は待たない)
3. `/compact` 後、1に戻る

## サブエージェント

Task / Agent ツールは原則禁止。
独立した領域に分割できる大規模な横断調査に限り使用可。
それ以外は直接ツールを使う。

## justfile

頻出手順は `justfile` にある。
対応レシピが分からない場合は `just --list` で確認し、
該当レシピがあればコマンドを組み立てず使用する。
`just` がなければ該当レシピの内容を直接実行する。
