# このリポジトリの運用ルール

## オリジナリティ

名前・造形・設定は既存タイトルから流用しない。市販タイトルの固有名詞を
md・コード・コミットメッセージに書かない。

## ディレクトリの使い分け

プランニングの成果は md ファイルで書き、下表のディレクトリに置く。

| ディレクトリ | 内容 |
|---|---|
| `design/` | 世界観・ストーリー・キャラクター設定など、実装を直接指示しない「設定資料」 |
| `plan/game/` | 3Dモデル・音以外の、ゲームシステム・機能ごとの仕様書。実装セッションがそのまま読んで手を付けられる粒度で書く |
| `plan/models/` | モンスター・キャラクター等、3Dモデル1体ごとの仕様書。同上 |
| `plan/sound/` | BGM・効果音・音声再生まわりの仕様書。音楽用セッションが担当する。同上 |

`plan/game/` `plan/models/` `plan/sound/` それぞれの下に、実装済みの
仕様書を集める `archive/` サブディレクトリがある(実装セッションが
移動・注記する運用)。

## PR の回し方

1. `just fresh <ブランチ名>`(同じブランチ名を使い回す)
2. 1つの決定・機能だけを編集し、`just ship "<メッセージ>"`
3. PR を作成し、レビューを待たず即マージ
4. `/compact` でコンテキストを圧縮(不可なら、マージ済みPRの詳細を
   持ち越さず要点だけ残す)して 1. に戻る

## サブエージェントは原則使わない

サブエージェント(Task / Agent ツール)の使用は、**作業が大規模で、かつ
調査領域を独立した範囲に分割できる場合のみ**許可する(例: 多数ファイルの
横断調査を領域ごとに並列で任せる)。それ以外での使用は禁止。数ファイルで
済む調査・単一ファイルの編集・逐次にしか進められない作業は、自分で直接
ツールを呼んで行う。

## justfile を必ず使う

頻出手順は `justfile` にレシピ化してある。まず `just --list` で一覧を確認し、
該当レシピがある作業はコマンド列を組み立てずレシピを使うこと。
`just` が無い環境では、justfile の該当レシピの中身をそのまま実行する。
