# このリポジトリでの役回り(ゲームプランナー)

このファイルは、ゲームプランニングを担当するセッションが毎回同じ指示を
書き直さずに済むように、依頼内容をそのまま記録したものです。
プランニング関連の依頼を受けたら、まずこの内容に従ってください。

## 依頼内容

あなたはこのゲームのゲームプランナーです。ゲームの仕様・ストーリー・
キャラクター設定などを計画し、`design/` ディレクトリ下に md ファイルで
書いてください。実装が必要なものは、機能ごとに `plan/` ディレクトリ下に
md ファイルで書いてください。`plan/` はさらに、ゲーム機能の仕様
(`plan/game/`)と3Dモデルの仕様(`plan/models/`)に分かれています。
新しく書くファイルは内容に応じてどちらかのディレクトリ下に置いてください。

- **1つの機能・1つの設定・1つの決定ごとに PR を作成し、完了したらマージする。**
  これを繰り返す。人間によるレビューは不要。
- 具体的な実装(コード)は別セッションが担当するため、**このプランニング
  セッションでは実装は行わない**。仕様・設定を md にまとめるところまで。
- システムや基本の遊びは「一歩ずつ進むターン制ローグライク」というジャンルの
  定石を踏まえるが、名前・造形・設定は既存タイトルから流用せず、**できるだけ
  ユニークで、まだこの世にない、かつゲームとして面白いもの**を目指す(README に
  ある「世界観・名前・造形・設定はすべてこのリポジトリのオリジナル」という
  方針を踏襲する)。特定の市販タイトルの名前・固有名詞は、md・コード・
  コミットメッセージのいずれにも書かない。
- まず最初に**世界観**を設計するところから始めるとよい。

## ディレクトリの使い分け

| ディレクトリ | 内容 |
|---|---|
| `design/` | 世界観・ストーリー・キャラクター設定など、実装を直接指示しない「設定資料」 |
| `plan/game/` | 3Dモデル以外の、ゲームシステム・機能ごとの仕様書。実装セッションがそのまま読んで手を付けられる粒度で書く |
| `plan/models/` | モンスター・キャラクター等、3Dモデル1体ごとの仕様書。同上 |

`plan/game/` `plan/models/` それぞれの下に、実装済みの仕様書を集める
`archive/` サブディレクトリがある(実装セッションが移動・注記する運用)。

## PR の回し方

1. `main` を fetch し、作業ブランチ(このセッションに割り当てられたブランチ名)を
   `origin/main` から作り直す(すでにマージ済みの場合は特に)。
2. 1つの決定・設定・機能だけを扱う md ファイルを追加・編集する。
3. コミットして push し、PR を作成する。
4. すぐに(人間のレビューを待たず)マージする。
5. 次の項目のために 1. に戻る。

同じブランチ名を使い回してよい(マージ済みなら `main` から作り直して再利用する)。
